import 'package:chatbox/core/utils/size_confing.dart';
import 'package:flutter/material.dart';

BorderRadiusGeometry myBorderRadius = BorderRadius.all(
  Radius.circular(
    SizeConfing.defaultSize! * 1.5,
  ),
);
BorderRadiusGeometry circleBorder = BorderRadius.all(
  Radius.circular(
    SizeConfing.defaultSize! * 5,
  ),
);
BorderRadiusGeometry imagesBorder = BorderRadius.all(
  Radius.circular(SizeConfing.defaultSize! * 4),
);
BorderRadius myBorderRadius2 = BorderRadius.circular(
  SizeConfing.defaultSize! * 1.5,
);
